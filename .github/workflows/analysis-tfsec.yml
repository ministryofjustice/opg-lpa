# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: tfsec

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  tfsec_analysis_to_github_security:
    uses: ministryofjustice/opg-github-workflows/.github/workflows/analysis-infrastructure-tfsec-to-github-security.yml@6bc0246b5cca2f4c85d06986c82a9a42643f6851 # v3.13.0
