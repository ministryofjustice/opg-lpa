<?xml version="1.0"?>
<psalm
    errorLevel="4"
    resolveFromConfigFile="true"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="https://getpsalm.org/schema/config"
    xsi:schemaLocation="https://getpsalm.org/schema/config vendor/vimeo/psalm/config.xsd"
>
    <projectFiles>
        <directory name="src/Opg/Lpa/Pdf" />
        <directory name="bin" />
        <ignoreFiles>
            <directory name="vendor" />
        </ignoreFiles>
    </projectFiles>

    <issueHandlers>
        <!--
            AbstractData defines properties as protected then adds getters for them. This means
            they're available publicly, but Psalm can't understand that.
        -->
        <InaccessibleProperty errorLevel="suppress" />

        <!-- We don't currently use override attributes -->
        <MissingOverrideAttribute errorLevel="suppress" />

        <!-- We magically fetch properties from the DataModel -->
        <UndefinedMagicPropertyFetch errorLevel="suppress" />
    </issueHandlers>
</psalm>
