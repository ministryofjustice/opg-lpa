<?php if ($this->lastLogin) : ?>
<div style="float:right;">You were last signed in on <?= $this->lastLogin ?></div>
<?php endif; ?>

<?php if($this->message):?>
<h3><?php echo $this->message?></h3>
<?php else:?>
    <?php if(empty($this->lpas)): ?>
    <p>You haven't created any LPAs yet.</p>
    <?php else: ?>
    <p>These are all the LPAs that you've created.</p>
    <?php endif;?>
<?php endif;?>

  <div style="margin: 5px;" >
    <a class="add button" href='<?php echo $this->url('user/dashboard/create-lpa') ?>' role="button">Create a new LPA</a>
    <div style="float:right;">
        <form action="/user/dashboard" method="POST" >
             <input type="text" name="freeText" style="width: 215px; " placeholder="LPA no, donor first or last name" value="<?php echo isset($this->freeText) ? $this->freeText: ''; ?>" />
             <input class = "add button" type="submit" value="Search" name="search"/>
             <a class="add button" id="clear" href="<?php echo $this->url('user/dashboard'); ?>"> Clear </a>
        </form>
    </div>
  </div>
  
  <?php if (!empty($this->lpas)) : ?>
  <table id="savedforms" class="lpa-list">
  <thead>
  <tr>
    <th>ID/Version</th>
    <th>Donor</th>
    <th>LPA</th>
    <th>Last saved on</th>
    <th>&nbsp;</th>
  </tr>
  </thead>
  <tbody>
  <?php foreach($this->lpas as $lpa): ?>
      <tr>
      <td><?php echo $lpa->id . ' v' . $lpa->version ?></td>
      <td><?php echo $lpa->donor; ?></td>
      <td><?php echo ($lpa->type == 'property-and-financial') ? 'Property and financial affairs' : 'Health and welfare'; ?></td>
      <td><?php echo $lpa->updatedAt->format('M jS, Y'); ?></td>
      <td>
        <div class="lpa-actions">
        <a class="button" href="<?= ($lpa->version == 2)?("/lpa/".$lpa->id):("/forward/lpa/".$lpa->id) ?>">Continue</a>
        <a class="button-warning delete-lpa" href="<?= ($lpa->version == 2)?("/user/dashboard/delete-lpa/".$lpa->id):("/service/delete/".$lpa->id) ?>">Delete</a>
        </div>
      </td>
      </tr>
  <?php endforeach;?>
  
  </tbody>
  </table>
  <p>
<?php endif; ?>
  
  