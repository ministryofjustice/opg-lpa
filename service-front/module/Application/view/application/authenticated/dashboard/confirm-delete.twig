{% extends isPopup ? 'layout/twig/popup.twig' : 'layout/twig/layout.twig' %}

{% block htmlTitle %}Delete this LPA - {% endblock %}
{% block pageTitle %}Delete an LPA{% endblock %}

{% block content %}

<header class="popup-header">
    <i class="icon icon-bin"></i>
    <h2 class="heading-small popup-header-title">Are you sure you want to delete this?</h2>
</header>

<div class="form">
    <h2 class="heading-medium">
        {% if donorName %}
            {{ donorName }}
        {% else %}
            No donor added
        {% endif %}
    </h2>
    <p>{{ formatLpaId(lpaId) }}</p>
    <p>This will only delete the information on this website. It will not delete any LPA application you've sent to OPG already, or any registered LPA.</p>
    <a href="/user/dashboard/delete-lpa/{{ lpaId }}" class="button button-warning flush--left">Delete</a>
    <a href="/user/dashboard" class="button button-secondary js-cancel">Cancel</a>
</div>


{% endblock %}
