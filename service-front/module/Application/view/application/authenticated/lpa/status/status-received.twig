{% extends 'layout/twig/layout.twig' %}

{% block htmlTitle %}Track your LPA - {% endblock %}
{% block pageTitle %}{{ lpa.document.donor.name }}{% endblock %}
{% block supportTitleBottom %}
    <span class="heading-secondary">
        {{ lpa.type == 'property-and-financial' ? 'Property and finance' :
        ((lpa.type == 'health-and-welfare') ? 'Health and welfare' : 'Not selected') }}:
        {{ formatLpaId(lpa.id) }}
    </span>
{% endblock %}

{% block content %}

    <div class="govuk-box-highlight opg-status--received">
        <div class="">
            <h2 class="heading-xlarge">We’ve received the LPA</h2>
        </div>
        <div>
            <p class="lede">When we've checked the LPA, we'll send a letter to the donor, the attorneys and the correspondent to let them know what happens next.</p>
            <p class="lede">It will take 8 to 10 weeks to register the LPA if there are no mistakes in the application.</p>
        </div>
        <div>
            <a class="heading-medium" style="color: #fff; display: inline-block" href="/user/dashboard">Back to your LPAs</a>
        </div>
    </div>

{% endblock content %}