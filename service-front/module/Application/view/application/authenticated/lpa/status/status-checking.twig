{% extends 'layout/twig/layout.twig' %}

{% block htmlTitle %}Track your LPA - {% endblock %}
{% block pageTitle %}{{ lpa.document.donor.name }}{% endblock %}
{% block supportTitleBottom %}
    <span class="heading-secondary">
        {{ lpa.document.type == 'property-and-financial' ? 'Property and finance' :
        (( lpa.document.type == 'health-and-welfare') ? 'Health and welfare' : 'Not selected') }}:
        {{ formatLpaId(lpa.id) }}
    </span>
{% endblock %}

{% block content %}

    <div class="govuk-box-highlight opg-status--checking">
        <div class="">
            <h2 class="heading-xlarge">We’re checking the LPA</h2>
        </div>
        <div>
            <p class="lede">We’re checking the LPA to make sure it’s been made correctly. Once we’ve checked it, we'll write to the donor, attorneys and correspondent to let them know what happens next.</p>
            <p class="lede">If there is something that must be corrected before the LPA can be registered, we'll contact the correspondent, {{ lpa.document.donor.name }}.</p>
            <p class="lede">The law says there must be a 4 week wait between the date OPG sent the ‘Notice of application’ letter to the donor and attorneys and the date the LPA is registered.</p>
        </div>
        <div>
            <a class="heading-medium" style="color: #fff; display: inline-block" href="/user/dashboard">Back to your LPAs</a>
        </div>
    </div>

{% endblock content %}