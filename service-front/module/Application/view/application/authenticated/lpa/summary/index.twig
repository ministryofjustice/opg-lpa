{% extends 'layout/layout.twig' %}

{% import "application/authenticated/dashboard/macros.twig" as macros %}

{%- set pageTitle = 'Review your details' -%}

{% block content %}

    <div class="grid-row">
        <div class="govuk-grid-column-full text">
            <p class="govuk-body-l">Use this section to check that the details of the people involved in the LPA are correct and to change the LPA if you need to.</p>

            <p class="govuk-body">You can also <a class="govuk-link" href="{{ url( 'lpa/download/draft', {'lpa-id': lpa.id, 'pdf-type':'lp1'} ) }}" rel="noopener noreferrer" target="_blank">download a preview of your LPA form (PDF, opens in new tab)</a> at this point, but do not send this version to the Office of the Public Guardian (OPG) for registration – it's incomplete.</p>

            <p class="govuk-body">Once you're sure that all the LPA details below are correct, fill in any remaining questions to complete the LPA. You'll then be able to print the LPA, sign it and post it to OPG.</p>
        </div>

    </div>

    <p class="govuk-body govuk-!-text-align-right">
        <a href="#" class="govuk-link js-print hide-if-no-js" data-analytics-click="page:link:Print this summary">
            <i class="icon icon-print" aria-hidden="true"></i>
            Print this summary
        </a>
    </p>

    <div class="grid-row">
        <div class="govuk-grid-column-full">
            {% include 'application/authenticated/lpa/summary/partials/summary-table.twig' %}
        </div>
    </div>

    <div class="grid-row">
        <div class="govuk-grid-column-full">
            <p class="govuk-body">
                <a href="{{ url(returnRoute, {'lpa-id': lpa.id}) }}" data-cy="continue" class="govuk-button">Continue</a>
            </p>
        </div>
    </div>

{% endblock %}
