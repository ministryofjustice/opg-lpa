{% extends 'layout/twig/creation-stage.twig' %}

{% block content %}

<section id="donor-section" class="section current">
	
	<h2 class="accordion-header">{{ accordionIdx(lpa) }}. Who is the donor for this LPA?</h2>
	
    <div class="accordion-content">
    		<div class="section-intro">
    			<p>The 'donor' is the person appointing other people to make decisions on their behalf. The donor:</p>
    
    			<ul>
    				<li>must be 18 or over</li>
    				<li>must be able to make their own decisions when they create the LPA – this is known as having mental capacity</li>
    			</ul>
    
    			<p>Only the donor can make decisions about their LPA and the people to involve.</p>
    
    
    			<p><a href="/guidance/#topic-donor" class="js-guidance" data-journey-click="stageprompt.lpa:help:donor">Find out more about being a donor</a></p>
    		</div>
    		
    {% if (lpa.document.donor) %}
    		<div class="person-summary group">
    			<div class="person">
    				<h3>{{ donor['name'] }}</h3>
    				<p>
    				{{ donor['address'] }}
    				</p>
    			</div>
    
    			<ul class="actions">
    				<li><a href="{{ editDonorUrl }}" data-form="donor" class="js-form-popup">View/edit details</a></li>
    			</ul>
    		</div>
    		<div class="accordion-actions group">
    			<p>
    				<a href="{{ nextRoute }}" class="button gotonextpart" role="button">Save and continue</a>
    			</p>
    		</div>

	{% else %}    
    
            <p>
                <a class="add button js-form-popup" data-form="donor" href="{{ addRoute }}" role="button">Add donor details</a>
            </p>
    		
    		<div class="accordion-actions group">
    			<p>
    				<span disabled="1" class="button">Save and continue</span>
    			</p>
    		</div>
    {% endif %}
    
    </div>

</section>

{% endblock %}