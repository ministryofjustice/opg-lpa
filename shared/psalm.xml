<?xml version="1.0"?>
<psalm
    errorLevel="4"
    resolveFromConfigFile="true"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="https://getpsalm.org/schema/config"
    xsi:schemaLocation="https://getpsalm.org/schema/config vendor/vimeo/psalm/config.xsd"
    ensureOverrideAttribute="false"
>
    <projectFiles>
        <directory name="module/MakeShared/src" />
        <ignoreFiles>
            <directory name="vendor" />
        </ignoreFiles>
    </projectFiles>

    <issueHandlers>
        <!--
            Everything in this module is designed to be used somewhere else, so Psalm cannot
            detect their usage
        -->
        <UnusedClass errorLevel="suppress" />
        <PossiblyUnusedMethod errorLevel="suppress" />

        <!--
            `final` classes are difficult to mock in tests and all of our code is internal,
            minimising its usefulness
        -->
        <ClassMustBeFinal errorLevel="suppress" />
    </issueHandlers>
</psalm>
