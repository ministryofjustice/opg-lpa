[project]
name = "pipelinescriptslib"
version = "1.0.0"
requires-python = "==3.14.5"
dependencies = []
[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
[tool.setuptools]
py-modules = ["ECSMonitor"]
